name: Streamlit Uygulamasini Uyanik Tut

on:
  schedule:
    # Her 4 saatte bir çalışır
    - cron: '0 */4 * * *'
  workflow_dispatch: 

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Uygulama Health Check Endpointine İstek Gönder
        # Streamlit'in sağlık kontrolü adresine istek atıyoruz. 
        # Bu yöntem yönlendirme (redirect) hatasına düşmez.
        run: |
          curl -sL "https://kibtek-data-knz7c69fis449g7rtdyxwk.streamlit.app/_stcore/health"
