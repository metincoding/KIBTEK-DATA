name: Streamlit Uygulamasini Uyanik Tut

on:
  schedule:
    - cron: '0 */4 * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Uygulamaya Istek Gonder
        run: |
          curl -s -o /dev/null -w "HTTP Durum Kodu: %{http_code}\n" https://kibtek-data-knz7c69fis449g7rtdyxwk.streamlit.app || true
